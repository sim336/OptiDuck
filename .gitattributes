# 文本文件在仓库内统一为 LF 行尾
* text=auto

# .pdf 显式声明为二进制。
# 本机 core.autocrlf=true（Git 安装级设置），而 git 的二进制嗅探只看前 8000 字节
# 有没有 NUL —— 头部是纯 ASCII 的 PDF（或内容其实是文本、只是后缀写成 .pdf 的文件）
# 会被误判成文本，行尾被改写，索引里的字节就和磁盘上的原文件不一致了。
*.pdf binary